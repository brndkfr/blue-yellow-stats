# Project instructions for Claude

## code.gs versioning

Whenever `web/tracker/code.gs` is modified, bump the `VERSION` constant at the top of the file.
The version follows the pattern `'v13'`, `'v14'`, etc. (increment by 1 each time).
