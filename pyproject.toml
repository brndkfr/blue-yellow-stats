[project]
name = "blue-yellow-stats"
version = "0.1.0"
description = "Game Stats and analytics"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "requests>=2.32",
  "rich>=15.0.0",
  "sqlite-utils>=3.37",
]
